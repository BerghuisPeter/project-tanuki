import { CharToColorDirective } from './char-to-color.directive';

describe('CharToColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CharToColorDirective();
    expect(directive).toBeTruthy();
  });
});
