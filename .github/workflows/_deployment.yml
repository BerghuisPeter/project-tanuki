on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
        description: Name of the target environment.

jobs:
  angular:
    name: Build Artefact Deploy - Angular
    uses: ./.github/workflows/_angular.yml
    secrets: inherit
    with:
      environment: ${{ inputs.environment }}
