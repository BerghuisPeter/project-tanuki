import { UuidColorDirective } from './uuid-color.directive';

describe('UuidColorDirective', () => {
  it('should create an instance', () => {
    const directive = new UuidColorDirective();
    expect(directive).toBeTruthy();
  });
});
